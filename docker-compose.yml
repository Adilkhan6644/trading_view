services:
  trading-alert-bot:
    build: .
    container_name: trading-alert-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
